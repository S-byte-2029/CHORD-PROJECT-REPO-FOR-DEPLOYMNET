{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate --noinput && python api/management/commands/seed_data.py && gunicorn chord_backend.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 3 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
